{
  "name": "assignment-b1-crud-snippets",
  "version": "1.0.0",
  "description": "In this assignment, you will create a web application to manage code [snippets](https://en.wikipedia.org/wiki/Snippet_(programming)). The web application will use the Node.js platform, Express as the application framework, and Mongoose as the object data modeling (ODM) library for MongoDB.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --inspect --env-file=.env src/server.js",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0",
    "test": "npx --node-options=--experimental-vm-modules jest || exit 0",
    "test:only": "npx --node-options=--experimental-vm-modules jest -t",
    "test:coverage": "npx --node-options=--experimental-vm-modules jest --coverage || exit 0"
  },
  "contributors": [
    "Ludwig Wittenberg <lw223cq@student.lnu.se>"
  ],
  "license": "MIT",
  "private": true,
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-ejs-layouts": "^2.5.1",
    "express-session": "^1.17.3",
    "helmet": "^8.0.0",
    "mongoose": "^8.0.4",
    "morgan": "^1.10.0",
    "@testing-library/dom": "^10.1.0",
    "@testing-library/jest-dom": "^6.4.5",
    "jest": "^29.7.0",
    "jest-chain": "^1.1.6",
    "jest-environment-jsdom": "^29.7.0",
    "jest-extended": "^4.0.2"
  },
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.10",
    "nodemon": "^3.0.2",
    "tailwindcss": "^3.4.14"
  }
}
